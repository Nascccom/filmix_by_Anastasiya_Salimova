import {createSlice} from "@reduxjs/toolkit";

const moviesReducer = createSlice({
    name: 'movies',
    initialState: {},
    reducers: {},

})


export const {} = moviesReducer.actions

export default moviesReducer.reducer